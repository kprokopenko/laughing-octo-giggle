Результирующий дамп в файле result.sql

В файле test_dump_2.db - база SqLite3.

Скрипт преобразования построен на правилах.
Правила строятся на основе регулярных выражений, с использованием небольших
синтаксических улучшений.

Правил добавил немного. Поэтому и разобралось тоже не всё.